{{- if .Values.webhooks.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "doca-provisioning.fullname" . }}-serving-cert
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "doca-provisioning.labels" . | nindent 4 }}
spec:
  dnsNames:
  - {{ include "doca-provisioning.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc
  - {{ include "doca-provisioning.fullname" . }}-webhook-service.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: {{ include "doca-provisioning.fullname" . }}-selfsigned-issuer
  secretName: {{ include "doca-provisioning.fullname" . }}-webhook-server-cert
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "doca-provisioning.fullname" . }}-selfsigned-issuer
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "doca-provisioning.labels" . | nindent 4 }}
spec:
  selfSigned: {}
{{- end }}
